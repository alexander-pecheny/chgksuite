name: Build Standalone Executables

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build and release (e.g., v0.26.1). Leave empty for build-only.'
        required: false
        default: ''

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            platform: linux-x64
            rust_target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            platform: linux-arm64
            rust_target: aarch64-unknown-linux-gnu
          - os: macos-15-intel
            platform: macos-x64
            rust_target: x86_64-apple-darwin
          - os: macos-latest
            platform: macos-arm64
            rust_target: aarch64-apple-darwin
          - os: windows-latest
            platform: windows-x64
            rust_target: x86_64-pc-windows-msvc

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout chgksuite
        uses: actions/checkout@v4

      - name: Checkout pyapp
        uses: actions/checkout@v4
        with:
          repository: ofek/pyapp
          path: pyapp
          ref: v0.29.0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Create build directory
        shell: bash
        run: mkdir -p artifacts

      # Generate platform-specific icon files from source PNG
      - name: Generate macOS icon (.icns)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          mkdir -p icon.iconset
          for size in 16 32 128 256 512; do
            sips -z $size $size icon.png --out "icon.iconset/icon_${size}x${size}.png" > /dev/null
            double=$((size * 2))
            sips -z $double $double icon.png --out "icon.iconset/icon_${size}x${size}@2x.png" > /dev/null
          done
          iconutil -c icns icon.iconset -o icon.icns
          rm -rf icon.iconset

      - name: Generate Windows icon (.ico)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          magick icon.png -define icon:auto-resize=256,48,32,16 icon.ico

      - name: Download rcedit (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          curl -L -o rcedit.exe https://github.com/electron/rcedit/releases/download/v2.0.0/rcedit-x64.exe

      # Determine versions and create requirements files
      - name: Setup build configuration
        id: config
        shell: bash
        run: |
          TAG="${{ inputs.tag || github.ref_name }}"

          # Check if this is a release build (tag) or dev build (branch)
          IS_RELEASE="false"
          if [[ "${{ github.ref }}" == refs/tags/* ]] || [[ -n "${{ inputs.tag }}" ]]; then
            IS_RELEASE="true"
          fi

          # Extract version from tag (remove 'v' prefix if present)
          if [[ "$TAG" =~ ^v ]]; then
            VERSION="${TAG:1}"
          else
            VERSION="$TAG"
          fi

          # Fallback to reading from version.py if no tag
          if [ -z "$VERSION" ] || [[ ! "$VERSION" =~ ^[0-9] ]]; then
            VERSION=$(grep -oP '__version__\s*=\s*"\K[^"]+' chgksuite/chgksuite/version.py 2>/dev/null || \
                      grep -o '__version__\s*=\s*"[^"]*"' chgksuite/chgksuite/version.py | cut -d'"' -f2 || \
                      echo "0.0.0")
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "is_release=$IS_RELEASE" >> $GITHUB_OUTPUT

          # For non-release builds, always use --pre to get latest beta versions
          if [ "$IS_RELEASE" = "false" ]; then
            PIP_PRE="--pre"
          elif [[ "$VERSION" =~ (a|b|rc)[0-9]+ ]]; then
            PIP_PRE="--pre"
          else
            PIP_PRE=""
          fi
          echo "pip_pre=$PIP_PRE" >> $GITHUB_OUTPUT

          echo "Build configuration:"
          echo "  Version: $VERSION"
          echo "  Is release: $IS_RELEASE"
          echo "  Pip pre flag: $PIP_PRE"

      # Create requirements files in pyapp directory to avoid Windows path issues
      - name: Create requirements files
        shell: bash
        run: |
          # Qt requirements
          echo "chgksuite" > pyapp/requirements-qt.txt
          echo "chgksuite-qt" >> pyapp/requirements-qt.txt

          # Tk requirements
          echo "chgksuite" > pyapp/requirements-tk.txt
          echo "chgksuite-tk" >> pyapp/requirements-tk.txt

          echo "=== Qt requirements ==="
          cat pyapp/requirements-qt.txt
          echo "=== Tk requirements ==="
          cat pyapp/requirements-tk.txt

      # Build Qt GUI (skip on linux-arm64 - no PyQt6 wheel)
      - name: Build Qt GUI
        if: matrix.platform != 'linux-arm64'
        shell: bash
        working-directory: pyapp
        run: |
          export PYAPP_PROJECT_NAME="chgksuite-qt"
          export PYAPP_PROJECT_VERSION="${{ steps.config.outputs.version }}"
          export PYAPP_PROJECT_DEPENDENCY_FILE="$(pwd)/requirements-qt.txt"
          export PYAPP_EXEC_SPEC="chgksuite_qt.__main__:main"
          export PYAPP_PYTHON_VERSION="3.12"
          export PYAPP_IS_GUI="1"
          export PYAPP_UV_ENABLED="1"
          export PYAPP_PIP_EXTRA_ARGS="${{ steps.config.outputs.pip_pre }}"
          export PYAPP_PASS_LOCATION="1"
          export PYAPP_EXPOSE_PYTHON="1"

          echo "Building Qt GUI with:"
          echo "  PYAPP_PROJECT_DEPENDENCY_FILE=$PYAPP_PROJECT_DEPENDENCY_FILE"
          echo "  PYAPP_PIP_EXTRA_ARGS=$PYAPP_PIP_EXTRA_ARGS"

          cargo build --release --target ${{ matrix.rust_target }}

          # Copy binary with platform-specific naming
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp target/${{ matrix.rust_target }}/release/pyapp.exe ../artifacts/chgkq-${{ matrix.platform }}.exe
            ../rcedit.exe ../artifacts/chgkq-${{ matrix.platform }}.exe --set-icon ../icon.ico
          else
            cp target/${{ matrix.rust_target }}/release/pyapp ../artifacts/chgkq-${{ matrix.platform }}
          fi

      # Build Tk GUI
      - name: Build Tk GUI
        shell: bash
        working-directory: pyapp
        run: |
          export PYAPP_PROJECT_NAME="chgksuite-tk"
          export PYAPP_PROJECT_VERSION="${{ steps.config.outputs.version }}"
          export PYAPP_PROJECT_DEPENDENCY_FILE="$(pwd)/requirements-tk.txt"
          export PYAPP_EXEC_SPEC="chgksuite_tk.__main__:main"
          export PYAPP_PYTHON_VERSION="3.12"
          export PYAPP_IS_GUI="1"
          export PYAPP_UV_ENABLED="1"
          export PYAPP_PIP_EXTRA_ARGS="${{ steps.config.outputs.pip_pre }}"
          export PYAPP_PASS_LOCATION="1"
          export PYAPP_EXPOSE_PYTHON="1"

          echo "Building Tk GUI with:"
          echo "  PYAPP_PROJECT_DEPENDENCY_FILE=$PYAPP_PROJECT_DEPENDENCY_FILE"
          echo "  PYAPP_PIP_EXTRA_ARGS=$PYAPP_PIP_EXTRA_ARGS"

          cargo build --release --target ${{ matrix.rust_target }}

          # Copy binary with platform-specific naming
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp target/${{ matrix.rust_target }}/release/pyapp.exe ../artifacts/chgkt-${{ matrix.platform }}.exe
            ../rcedit.exe ../artifacts/chgkt-${{ matrix.platform }}.exe --set-icon ../icon.ico
          else
            cp target/${{ matrix.rust_target }}/release/pyapp ../artifacts/chgkt-${{ matrix.platform }}
          fi

      # Ship .desktop files and icon for Linux
      - name: Create Linux desktop entries
        if: runner.os == 'Linux'
        shell: bash
        run: |
          cp icon.png artifacts/chgksuite.png

          # Qt .desktop (skip on linux-arm64 where Qt is not built)
          if [ -f "artifacts/chgkq-${{ matrix.platform }}" ]; then
            cat > "artifacts/chgkq.desktop" << 'DESKTOP'
          [Desktop Entry]
          Type=Application
          Name=chgksuite (Qt)
          Comment=CHGK quiz package toolkit
          Exec=chgkq
          Icon=chgksuite
          Categories=Utility;Education;
          Terminal=false
          DESKTOP
          fi

          # Tk .desktop
          cat > "artifacts/chgkt.desktop" << 'DESKTOP'
          [Desktop Entry]
          Type=Application
          Name=chgksuite (Tk)
          Comment=CHGK quiz package toolkit
          Exec=chgkt
          Icon=chgksuite
          Categories=Utility;Education;
          Terminal=false
          DESKTOP

      # Create macOS .app bundles (required for Finder launch)
      - name: Create macOS app bundles
        if: runner.os == 'macOS'
        shell: bash
        run: |
          # Create Qt .app bundle
          if [ -f "artifacts/chgkq-${{ matrix.platform }}" ]; then
            mkdir -p "artifacts/chgkq.app/Contents/MacOS"
            mkdir -p "artifacts/chgkq.app/Contents/Resources"
            mv "artifacts/chgkq-${{ matrix.platform }}" "artifacts/chgkq.app/Contents/MacOS/chgkq"
            cp icon.icns "artifacts/chgkq.app/Contents/Resources/icon.icns"
            cat > "artifacts/chgkq.app/Contents/Info.plist" << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleExecutable</key>
            <string>chgkq</string>
            <key>CFBundleIdentifier</key>
            <string>me.pecheny.chgkq</string>
            <key>CFBundleIconFile</key>
            <string>icon</string>
            <key>CFBundleName</key>
            <string>chgksuite-qt</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>CFBundleVersion</key>
            <string>${{ steps.config.outputs.version }}</string>
            <key>CFBundleShortVersionString</key>
            <string>${{ steps.config.outputs.version }}</string>
            <key>LSMinimumSystemVersion</key>
            <string>10.15</string>
            <key>NSHighResolutionCapable</key>
            <true/>
          </dict>
          </plist>
          EOF
            # Ad-hoc sign the app bundle to prevent "damaged" error
            codesign --force --deep --sign - "artifacts/chgkq.app"
            # Zip the .app bundle
            cd artifacts && zip -r "chgkq-${{ matrix.platform }}.zip" chgkq.app && rm -rf chgkq.app && cd ..
          fi

          # Create Tk .app bundle
          if [ -f "artifacts/chgkt-${{ matrix.platform }}" ]; then
            mkdir -p "artifacts/chgkt.app/Contents/MacOS"
            mkdir -p "artifacts/chgkt.app/Contents/Resources"
            mv "artifacts/chgkt-${{ matrix.platform }}" "artifacts/chgkt.app/Contents/MacOS/chgkt"
            cp icon.icns "artifacts/chgkt.app/Contents/Resources/icon.icns"
            cat > "artifacts/chgkt.app/Contents/Info.plist" << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleExecutable</key>
            <string>chgkt</string>
            <key>CFBundleIdentifier</key>
            <string>me.pecheny.chgkt</string>
            <key>CFBundleIconFile</key>
            <string>icon</string>
            <key>CFBundleName</key>
            <string>chgksuite-tk</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
            <key>CFBundleVersion</key>
            <string>${{ steps.config.outputs.version }}</string>
            <key>CFBundleShortVersionString</key>
            <string>${{ steps.config.outputs.version }}</string>
            <key>LSMinimumSystemVersion</key>
            <string>10.15</string>
            <key>NSHighResolutionCapable</key>
            <true/>
          </dict>
          </plist>
          EOF
            # Ad-hoc sign the app bundle to prevent "damaged" error
            codesign --force --deep --sign - "artifacts/chgkt.app"
            # Zip the .app bundle
            cd artifacts && zip -r "chgkt-${{ matrix.platform }}.zip" chgkt.app && rm -rf chgkt.app && cd ..
          fi

      - name: List artifacts
        shell: bash
        run: ls -lh artifacts/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: builds-${{ matrix.platform }}
          path: artifacts/*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || inputs.tag != ''
    permissions:
      contents: write

    steps:
      - name: Get tag message
        id: tag_message
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ inputs.tag || github.ref_name }}"
          # Get annotated tag message via API (git checkout mangles tag objects)
          MSG=$(gh api repos/${{ github.repository }}/git/refs/tags/"$TAG" --jq '.object.sha' \
            | xargs -I{} gh api repos/${{ github.repository }}/git/tags/{} --jq '.message' 2>/dev/null || true)
          if [ -z "$MSG" ]; then
            echo "body=" >> $GITHUB_OUTPUT
            echo "use_auto=true" >> $GITHUB_OUTPUT
          else
            {
              echo "body<<TAGMSG_EOF"
              echo "$MSG"
              echo "TAGMSG_EOF"
            } >> $GITHUB_OUTPUT
            echo "use_auto=false" >> $GITHUB_OUTPUT
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: ls -la artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          files: artifacts/*
          body: ${{ steps.tag_message.outputs.body }}
          generate_release_notes: ${{ steps.tag_message.outputs.use_auto == 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
